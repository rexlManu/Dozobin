import { AppProviders } from '@/components/app-providers';
import { ResetRoute } from '@/screens/auth';

export default function ResetPage() {
    return (
        <AppProviders>
            <ResetRoute />
        </AppProviders>
    );
}
